import { styled } from "styled-components";
const ProductTitle=styled.h2`
    font-size: 1.125rem;
    font-weight:bold;
    color: rgba(0,0,0,.87);
    // margin: 0.2em;
`;
export default ProductTitle;